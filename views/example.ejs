
<!DOCTYPE html>

<html>
    <head>
        <title>Example</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="keywords" content="website, html">
        <meta name="author" content="Victor Persson">
    <body>
        <% for (task of list) { %>
        <div>
            <p id="<%= task._id %>">Note: <%= task.note %></p>
        </div>
        <% } %>
    </body>
</html>
